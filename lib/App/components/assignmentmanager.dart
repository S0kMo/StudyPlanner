import 'package:flutter/material.dart';

class AssignmentManager extends StatelessWidget {
  const AssignmentManager({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
